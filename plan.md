# JAB Code 无损传输方案（修订版）

## 目标
将任意源码文件通过“压缩 → 分块 → JAB Code 编码 → 多张 PNG”链路无损还原。实际分块按压缩后字节数，不按行数；代码大小不固定时同样适用。

## 单码参数
| 参数 | 取值 |
|---|---|
| 颜色数 | 8 色（3 bits/模块） |
| 符号版本 | 24×24（113×113 模块） |
| 纠错等级 | ECC4（`--ecc-level 4`） |
| 模块尺寸 | 16 px |
| 单图尺寸 | 1808×1808 px |
| 实测单码最大二进制净荷 | 2310 字节 |
| 每页安全净荷 | 2200 字节（保留 110 字节余量给页眉和模式切换开销） |

## 页数据格式
每页生成一个独立的 JAB Code PNG。

```
[4 字节]  Magic "JABP"
[4 字节]  当前页索引（从 0 开始）
[4 字节]  总页数 N
[2 字节]  本页压缩数据长度 L
[4 字节]  本页压缩数据 CRC32
[L 字节]  gzip 压缩数据
```

第 0 页的压缩数据开头包含全局头：

```
[4 字节]  Magic "JAB0"
[8 字节]  原始文件未压缩大小（uint64）
[32 字节] 原始文件 SHA-256
[2 字节]  文件名长度
[变长]    文件名
[...]     后续为源码的 gzip 压缩数据
```

## 编解码流程

### 编码
1. 读入源码，计算 SHA-256。
2. 构造“全局头 + 源码”作为待压缩流。
3. 将流切分为多块，每块独立 gzip 压缩后 ≤ 2200 字节。单块压缩若超过阈值，则缩小输入再压缩，确保可装入一页。
4. 为每块加上页眉，生成 `page_xxx.bin`。
5. 调用 `jabcodeWriter` 将每页二进制数据编码为 `page_xxx.png`。

### 解码
1. 调用 `jabcodeReader` 解码每张 PNG，得到 `page_xxx.bin`。
2. 校验 Magic、CRC32，按页索引排序拼接压缩流。
3. 逐个 gzip 解压并拼接原始字节。
4. 从头部解析全局头，分离文件名与源码。
5. 计算源码 SHA-256，与全局头比对，确认无损。

## 输出物
- `encode_to_jab.py`：输入源码文件，输出编号 PNG。
- `decode_from_jab.py`：输入 PNG 目录，还原源码并校验 SHA-256。

## 实测目标
使用 `src/jabcode/decoder.c` 做端到端测试：编码 → 解码 → SHA-256 一致。
